load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library", "cc_test")

cc_library(
    name = "clpwrapper",
    srcs = [file for file in glob(["*.cpp"])],
    hdrs = [file for file in glob(["*.hpp"])],
    # strip_include_prefix = "src",
    deps = ["@eigen", "@libCoinUtils", "@libClp"],
    visibility = ["//visibility:public"],
)





