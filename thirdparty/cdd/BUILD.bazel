load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library", "cc_test")

cc_library(
    name = "myCdd",
    srcs = [file for file in glob(["src/*.c", "src/*.cpp"])],
    hdrs = [file for file in glob(["src/*.h", "src/*.hpp"])],
    strip_include_prefix = "src",
    deps = ["@eigen"],
    visibility = ["//visibility:public"],
)